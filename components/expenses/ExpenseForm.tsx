'use client'
import type {FormEvent} from 'react'; import {F,Sel,TA,Save} from '@/components/common/FormFields'; import {today} from '@/lib/academy/format'
export default function ExpenseForm({profile,onSave}:any){function submit(e:FormEvent<HTMLFormElement>){e.preventDefault(); const o=Object.fromEntries(new FormData(e.currentTarget) as any); onSave({...o,amount:Number(o.amount),registered_by:profile.id})} return <form onSubmit={submit}><div className="formGrid"><Sel name="category" label="Categoría" value="payroll" options={['payroll','rent','utilities','costumes','maintenance','advertising','other']}/><F name="amount" label="Importe *" type="number" required/><F name="expense_date" label="Fecha" type="date" value={today()}/><F name="description" label="Descripción"/><Sel name="payment_method" label="Método" value="transfer" options={['cash','transfer','card','other']}/><F name="reference" label="Referencia"/><TA name="notes" label="Notas"/><Save/></div></form>}
