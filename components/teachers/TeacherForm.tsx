'use client'
import type {FormEvent} from 'react'; import {F,Sel,TA,Save} from '@/components/common/FormFields'
export default function TeacherForm({row={},onSave}:any){function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();onSave(Object.fromEntries(new FormData(e.currentTarget) as any),row.id)} return <form onSubmit={submit}><div className="formGrid"><F name="full_name" label="Nombre completo *" value={row.full_name} required/><F name="phone" label="Teléfono" value={row.phone}/><F name="email" label="Correo" type="email" value={row.email}/><F name="main_discipline" label="Disciplina principal" value={row.main_discipline}/><Sel name="payment_scheme" label="Esquema de pago" value={row.payment_scheme||'monthly'} options={['monthly','per_class','per_hour','per_student','percentage']}/><F name="base_pay" label="Importe / nómina base" type="number" value={row.base_pay||0}/><Sel name="status" label="Estado" value={row.status||'active'} options={['active','inactive']}/><TA name="notes" label="Notas" value={row.notes}/><Save/></div></form>}
