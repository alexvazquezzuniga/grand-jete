'use client'
import type {FormEvent} from 'react'; import {F,Sel,TA,Save} from '@/components/common/FormFields'; import {today} from '@/lib/academy/format'
export default function StudentForm({row={},onSave}:any){function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();onSave(Object.fromEntries(new FormData(e.currentTarget) as any),row.id)} return <form onSubmit={submit}><div className="formGrid"><F name="full_name" label="Nombre completo *" value={row.full_name} required/><F name="birth_date" label="Fecha de nacimiento" type="date" value={row.birth_date}/><F name="phone" label="Teléfono" value={row.phone}/><F name="email" label="Correo" type="email" value={row.email}/><F name="guardian_name" label="Tutor / responsable" value={row.guardian_name}/><F name="guardian_phone" label="Teléfono tutor" value={row.guardian_phone}/><F name="emergency_contact" label="Contacto de emergencia" value={row.emergency_contact}/><F name="emergency_phone" label="Teléfono emergencia" value={row.emergency_phone}/><F name="enrollment_date" label="Fecha de ingreso" type="date" value={row.enrollment_date||today()}/><Sel name="status" label="Estado" value={row.status||'active'} options={['active','paused','inactive']}/><TA name="notes" label="Notas" value={row.notes}/><Save/></div></form>}
